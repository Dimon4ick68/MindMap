# MindApp — Mind Mapping Software

Курсова робота з дисципліни "Технології розроблення програмного забезпечення".
Система для створення ментальних карт з підтримкою мультимедіа, вкладок та хмарної синхронізації.

## 📋 Вимоги до системи

Для запуску проєкту на вашому комп'ютері повинні бути встановлені:
1.  **Java JDK 17** або новіше.
2.  **PostgreSQL** (сервер бази даних).
3.  **Git** (для клонування репозиторію).

---

## 🛠️ Крок 1. Налаштування Бази Даних

Перед запуском сервера необхідно підготувати базу даних.

1.  Відкрийте **pgAdmin**, **DBeaver** або термінал PostgreSQL (`psql`).
2.  За замовчуванням сервер налаштований на використання системної бази `postgres`. Переконайтеся, що вона доступна, або створіть нову базу `mindapp_db`.
3.  **Налаштування підключення:**
    Відкрийте файл `server/src/main/resources/application.properties` і перевірте налаштування:
    ```properties
    spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
    spring.datasource.username=postgres
    spring.datasource.password=12345678
    ```
    *Якщо ваш пароль до PostgreSQL відрізняється від `12345678`, змініть його в цьому файлі.*

> **Примітка:** Таблиці (`users`, `mind_maps`, `nodes`) будуть створені автоматично при першому запуску сервера (завдяки Hibernate `ddl-auto=update`).

---

## 🚀 Крок 2. Запуск Сервера (Backend)

Сервер відповідає за бізнес-логіку та зв'язок з БД.

1.  Відкрийте термінал у кореневій папці проєкту.
2.  Перейдіть у папку сервера:
    ```bash
    cd server
    ```
3.  Запустіть сервер (використовуючи Maven Wrapper):
    * **Windows:**
      ```cmd
      .\mvnw.cmd spring-boot:run
      ```
    * **Linux / macOS:**
      ```bash
      ./mvnw spring-boot:run
      ```

Дочекайтеся повідомлення в консолі: `Tomcat started on port 8081`.
*Сервер працюватиме за адресою: http://localhost:8081*

---

## 🖥️ Крок 3. Запуск Клієнта (Frontend)

Клієнт — це настільний додаток на JavaFX.

1.  Відкрийте **новий** термінал (не закриваючи термінал сервера).
2.  Перейдіть у папку клієнта:
    ```bash
    cd client/demo
    ```
3.  Запустіть клієнт:
    * **Windows:**
      ```cmd
      .\mvnw.cmd javafx:run
      ```
      *(Примітка: якщо у папці клієнта немає файлу `mvnw.cmd`, ви можете скопіювати його з папки `server` або використати встановлений у системі Maven командою `mvn javafx:run`)*.
    * **Linux / macOS:**
      ```bash
      ./mvnw javafx:run
      ```

---

## 📖 Як користуватися (Короткий гайд)

1.  **Авторизація:**
    * При першому запуску натисніть **"Немає акаунту? Зареєструватись"**.
    * Придумайте логін (наприклад, `admin`) та пароль.
    * Увійдіть у систему.

2.  **Створення мапи:**
    * Натисніть **"Створити нову мапу"**.
    * Відкриється редактор у новій вкладці.

3.  **Редагування:**
    * **ПКМ (Права кнопка миші)** по вузлу — контекстне меню (редагувати текст, додати дочірній вузол, видалити).
    * **ПКМ по порожньому місцю** — переміщення (панорамування) полотна.
    * **ЛКМ (Ліва кнопка)** — виділення та перетягування вузлів.
    * **Ctrl + Коліщатко миші** — масштабування (Zoom).
    * **Виділення ласо:** Затисніть ЛКМ на порожньому місці й потягніть.

4.  **Мультимедіа:**
    * Використовуйте кнопки на панелі для додавання фото або відео до вузлів.
    * Подвійний клік по вузлу з медіа відкриває перегляд.

---

## 🏗️ Архітектура та Технології

* **Архітектура:** Клієнт-Сервер (SOA), REST API.
* **Backend:** Java 17, Spring Boot, Spring Data JPA, PostgreSQL.
* **Frontend:** JavaFX, HTTP Client.
* **Патерни проєктування:** Composite, Strategy, Bridge, Abstract Factory, Prototype, Repository.